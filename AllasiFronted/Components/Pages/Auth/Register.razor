@page "/register"
@using System.ComponentModel.DataAnnotations
@using Progra3_Frontend.Model
@using Progra3_Frontend.Services
@using Progra3_Frontend.Services.Auth
@using AllasiFrontend.Components.Shared.ui
@inject NavigationManager Navigation
@inject ClientesRS clienteRS
@inject CustomAuthenticationStateProvider AuthProvider
@inject ConfirmDialogService DialogService
@inject Microsoft.AspNetCore.Hosting.IWebHostEnvironment Env


<div class="container">
    <section>
        <div class="form-header">
            <h1 class="title">
                CREAR CUENTA
            </h1>
            <h3 class="sub-title">
                Completa tus datos para crear tu cuenta.
            </h3>
            <div class="hr"></div>
        </div>

        <EditForm Model="formUser" OnValidSubmit="CrearCuenta" OnInvalidSubmit="HandleInvalidSubmit" class="form-body">
            <DataAnnotationsValidator />
            
            @if (!string.IsNullOrEmpty(errorMessage))
            {
                <div class="alert alert-danger" style="color: red; margin-bottom: 15px; font-weight: bold;" role="alert">
                    @errorMessage
                </div>
            }

            <div class="register-container">
                <div class="left-column">
                    <div class="form-input">
                        <label>Nombre(s)</label>
                        <div class="input-container">
                            <i class="bi bi-person"></i>
                            <InputText @bind-Value="formUser.Nombres" placeholder="Ingresa tu nombre" />
                        </div>
                        <ValidationMessage For="@(() => formUser.Nombres)" />
                    </div>

                    <div class="double-row">
                        <div class="form-input">
                            <label>Apellido Paterno</label>
                            <div class="input-container">
                                <i class="bi bi-person"></i>
                                <InputText @bind-Value="formUser.ApellidoPaterno" placeholder="Apellido paterno" />
                            </div>
                            <ValidationMessage For="@(() => formUser.ApellidoPaterno)" />
                        </div>

                        <div class="form-input">
                            <label>Apellido Materno</label>
                            <div class="input-container">
                                <i class="bi bi-person"></i>
                                <InputText @bind-Value="formUser.ApellidoMaterno" placeholder="Apellido materno" />
                            </div>
                            <ValidationMessage For="@(() => formUser.ApellidoMaterno)" />
                        </div>
                    </div>

                    <div class="form-input">
                        <label>Correo electrónico</label>
                        <div class="input-container">
                            <i class="bi bi-envelope"></i>
                            <InputText @bind-Value="formUser.Email" placeholder="ejemplo@correo.com" />
                        </div>
                        <ValidationMessage For="@(() => formUser.Email)" />
                    </div>

                    <div class="form-input">
                        <label>Contraseña</label>
                        <div class="input-container" style="position: relative;">
                            <i class="bi bi-lock-fill"></i>
                            <input @bind-Value="formUser.Password" @bind-Value:event="oninput" type="@(showPassword ? "text" : "password")" placeholder="Crea una contraseña" style="padding-right: 35px; width: 100%;" />
                            <i class="bi @(showPassword ? "bi-eye-slash" : "bi-eye") text-muted" style="position: absolute; right: 15px; cursor: pointer;" @onclick="() => showPassword = !showPassword"></i>
                        </div>
                        <ValidationMessage For="@(() => formUser.Password)" />
                        
                        <div class="password-strength-container">
                            <div class="password-strength-bars">
                                <div class="strength-bar @GetStrengthBarClass(1)"></div>
                                <div class="strength-bar @GetStrengthBarClass(2)"></div>
                                <div class="strength-bar @GetStrengthBarClass(3)"></div>
                                <div class="strength-bar @GetStrengthBarClass(4)"></div>
                                <div class="strength-bar @GetStrengthBarClass(5)"></div>
                            </div>
                            <span class="strength-text @GetStrengthTextClass()">@GetStrengthText()</span>
                        </div>
                        
                        <div class="requirements-box mt-2 p-2 bg-light" style="border-radius: 8px; width: 100%;">
                            <div class="row g-1" style="font-size: 0.75rem;">
                                <div class="col-sm-6">
                                    <i class="bi @(hasMinLength ? "bi-check-circle-fill text-success" : "bi-x-circle text-danger") me-1"></i> Mínimo 8 caracteres
                                </div>
                                <div class="col-sm-6">
                                    <i class="bi @(hasNumber ? "bi-check-circle-fill text-success" : "bi-x-circle text-danger") me-1"></i> Al menos un número
                                </div>
                                <div class="col-sm-6">
                                    <i class="bi @(hasUpper ? "bi-check-circle-fill text-success" : "bi-x-circle text-danger") me-1"></i> Al menos una mayúscula
                                </div>
                                <div class="col-sm-6">
                                    <i class="bi @(hasSpecial ? "bi-check-circle-fill text-success" : "bi-x-circle text-danger") me-1"></i> Al menos un carácter especial
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="form-input">
                        <label>Repetir Contraseña</label>
                        <div class="input-container" style="position: relative;">
                            <i class="bi bi-lock-fill"></i>
                            <InputText @bind-Value="formUser.ConfirmPassword" type="@(showConfirmPassword ? "text" : "password")" placeholder="Repite tu contraseña" style="padding-right: 35px; width: 100%;" />
                            <i class="bi @(showConfirmPassword ? "bi-eye-slash" : "bi-eye") text-muted" style="position: absolute; right: 15px; cursor: pointer;" @onclick="() => showConfirmPassword = !showConfirmPassword"></i>
                        </div>
                        <ValidationMessage For="@(() => formUser.ConfirmPassword)" />
                    </div>
                </div>

                <div class="right-column">
                    <div class="form-input">
                        <label>DNI</label>
                        <div class="input-container">
                            <i class="bi bi-person-vcard"></i>
                            <InputText @bind-Value="formUser.Dni" placeholder="Ingresa tu DNI" maxlength="8" />
                        </div>
                        <ValidationMessage For="@(() => formUser.Dni)" />
                    </div>

                    <div class="form-input">
                        <label>Teléfono</label>
                        <div class="input-container">
                            <i class="bi bi-telephone"></i>
                            <InputText @bind-Value="formUser.Telefono" placeholder="Ingresa tu teléfono" maxlength="9" />
                        </div>
                        <ValidationMessage For="@(() => formUser.Telefono)" />
                    </div>

                    <div class="form-input">
                        <label>Fecha de Nacimiento</label>
                        <div class="input-container">
                            <i class="bi bi-calendar3"></i>
                            <InputDate @bind-Value="formUser.FechaNacimiento" />
                        </div>
                        <ValidationMessage For="@(() => formUser.FechaNacimiento)" />
                    </div>
                </div>
            </div>

            @if (isProcessing)
            {
                <button type="button" class="register-button" disabled style="opacity: 0.7; cursor: not-allowed;">
                    <span class="spinner-border spinner-border-sm" role="status" aria-hidden="true" style="margin-right: 8px;"></span>
                    Creando Cuenta...
                </button>
            }
            else
            {
                <button type="submit" class="register-button">
                    Crear Cuenta
                </button>
            }

            <div class="footer-link">
                <span class="label">¿Ya tienes cuenta?</span>
                <a href="/login" class="link"><u>Iniciar sesión</u></a>
            </div>
        </EditForm>
    </section>
</div>

@if (showTermsModal)
{
    <div class="custom-modal-backdrop" style="z-index: 10000;" @onclick="CancelTerms">
        <div class="custom-modal-dialog" @onclick:stopPropagation="true">
            <div class="custom-modal-content">
                <div class="custom-modal-icon icon-bg-info">
                    <i class="bi bi-info-circle"></i>
                </div>
                <h3 class="custom-modal-title">Términos y Condiciones</h3>
                <p class="custom-modal-message">
                    ¿Aceptas los términos y condiciones?
                    <br /><br />
                    <a href="javascript:void(0)" style="color:#0ea5e9; text-decoration:underline; font-weight:bold;" @onclick="ShowDocument">Ver términos y condiciones</a>
                </p>
                <div class="custom-modal-actions">
                    <button class="btn-custom-cancel" @onclick="CancelTerms">Cancelar</button>
                    <button class="btn-custom-confirm btn-confirm-info" @onclick="ConfirmTerms">Aceptar</button>
                </div>
            </div>
        </div>
    </div>

    <style>
        .custom-modal-backdrop { position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; background-color: rgba(15, 23, 42, 0.5); backdrop-filter: blur(4px); z-index: 9999; display: flex; align-items: center; justify-content: center; }
        .custom-modal-dialog { background-color: #ffffff; border-radius: 24px; width: 90%; max-width: 400px; padding: 32px 24px; box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25); text-align: center; }
        .custom-modal-icon { width: 72px; height: 72px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 2.2rem; margin: 0 auto 20px auto; }
        .icon-bg-info { background-color: #e0f2fe; color: #0ea5e9; }
        .custom-modal-title { font-size: 1.35rem; font-weight: 700; color: #0f172a; margin-bottom: 12px; }
        .custom-modal-message { font-size: 0.95rem; color: #64748b; margin-bottom: 30px; line-height: 1.5; }
        .custom-modal-actions { display: flex; gap: 12px; justify-content: center; }
        .btn-custom-cancel, .btn-custom-confirm { padding: 12px 20px; border-radius: 14px; font-weight: 600; font-size: 0.95rem; border: none; cursor: pointer; flex: 1; }
        .btn-custom-cancel { background-color: #f1f5f9; color: #475569; }
        .btn-confirm-info { background-color: #0ea5e9; color: white; }
    </style>
}

@if (showDocumentModal)
{
    <div class="custom-modal-backdrop" style="z-index: 10001;" @onclick="HideDocument">
        <div class="custom-modal-dialog" style="max-width: 800px; width: 90%; height: 80vh; display: flex; flex-direction: column;" @onclick:stopPropagation="true">
            <div class="custom-modal-content" style="flex: 1; display: flex; flex-direction: column; height: 100%;">
                <h3 class="custom-modal-title" style="margin-bottom: 15px;">Términos y Condiciones y Política de Privacidad</h3>
                <div style="flex: 1; overflow: hidden; border-radius: 8px; border: 1px solid #e2e8f0; height: calc(100% - 100px); background: #f8fafc; text-align: left; overflow-y: auto;">
                    @((MarkupString)termsHtmlContent)
                </div>
                <div class="custom-modal-actions" style="margin-top: 20px;">
                    <button class="btn-custom-confirm btn-confirm-info" style="width: 100%; max-width: 200px; margin: 0 auto;" @onclick="HideDocument">Cerrar</button>
                </div>
            </div>
        </div>
    </div>
}

@code {
    private RegistroUsuario formUser = new();
    
    private string? errorMessage;
    private string termsHtmlContent = string.Empty;

    protected override async Task OnInitializedAsync()
    {
        try
        {
            var path = System.IO.Path.Combine(Env.WebRootPath, "docs", "Terminos_y_Condiciones.html");
            if (System.IO.File.Exists(path))
            {
                termsHtmlContent = await System.IO.File.ReadAllTextAsync(path);
            }
            else
            {
                termsHtmlContent = "<p style='padding: 20px;'>No se encontraron los términos y condiciones.</p>";
            }
        }
        catch
        {
            termsHtmlContent = "<p style='padding: 20px;'>Error al cargar los términos y condiciones.</p>";
        }
    }

    private bool isProcessing = false;
    private bool showPassword = false;
    private bool showConfirmPassword = false;
    private bool showTermsModal = false;
    private bool showDocumentModal = false;

    private int PasswordScore => CalculatePasswordScore(formUser.Password);

    private string PasswordToCheck => formUser.Password ?? "";
    private bool hasMinLength => PasswordToCheck.Length >= 8;
    private bool hasNumber => PasswordToCheck.Any(char.IsDigit);
    private bool hasUpper => PasswordToCheck.Any(char.IsUpper);
    private bool hasSpecial => PasswordToCheck.Any(ch => !char.IsLetterOrDigit(ch));

    private void HandleInvalidSubmit()
    {
        errorMessage = "Por favor, complete correctamente todos los campos y cumpla los requisitos de seguridad de la contraseña.";
    }

    private int CalculatePasswordScore(string? password)
    {
        if (string.IsNullOrEmpty(password)) return 0;
        int score = 0;
        if (password.Length >= 8) score++;
        if (password.Any(char.IsUpper)) score++;
        if (password.Any(char.IsLower)) score++;
        if (password.Any(char.IsDigit)) score++;
        if (password.Any(ch => !char.IsLetterOrDigit(ch))) score++;
        return score;
    }

    private string GetStrengthBarClass(int barIndex)
    {
        var score = PasswordScore;
        if (barIndex > score) return "";

        return score switch
        {
            1 => "weak",
            2 => "fair",
            3 => "good",
            4 => "strong",
            5 => "very-strong",
            _ => ""
        };
    }

    private string GetStrengthTextClass()
    {
        return PasswordScore switch
        {
            0 => "text-empty",
            1 => "text-weak",
            2 => "text-fair",
            3 => "text-good",
            4 => "text-strong",
            5 => "text-very-strong",
            _ => "text-empty"
        };
    }

    private string GetStrengthText()
    {
        return PasswordScore switch
        {
            0 => "Nivel de seguridad",
            1 => "Muy Débil",
            2 => "Débil",
            3 => "Aceptable",
            4 => "Fuerte",
            5 => "Muy Fuerte",
            _ => ""
        };
    }

    private void CrearCuenta()
    {
        errorMessage = null;
        if (isProcessing) return;
       if (PasswordScore < 4) 
        {
            errorMessage = "La contraseña no cumple con todos los requisitos de seguridad.";
             return;
         }
        showTermsModal = true;
    }

    private void ShowDocument() => showDocumentModal = true;
    private void HideDocument() => showDocumentModal = false;

    private void CancelTerms()
    {
        showTermsModal = false;
        isProcessing = false;
    }

    private async Task ConfirmTerms()
    {
        showTermsModal = false;
        isProcessing = true;
        errorMessage = null;

        try
        {
            Cliente cliente = new();
        cliente.dni = formUser.Dni;
        cliente.correo = formUser.Email;
        cliente.nombre = formUser.Nombres;
        cliente.apellidoCompleto = formUser.ApellidoPaterno + " " + formUser.ApellidoMaterno;
        cliente.contrasenaHash = formUser.Password; //TODO: Encriptar mensaje
        cliente.fechaNacimiento = formUser.FechaNacimiento;
        cliente.telefono = formUser.Telefono; 
        
        Cliente? nuevoCliente = await clienteRS.InsertarAsync(cliente);

        if (nuevoCliente == null)
        {
            errorMessage = "Hubo un problema al crear la cuenta. Verifica tus datos o intenta más tarde.";
        }
        else
        {
            await AuthProvider.Login(
                nuevoCliente.idUsuario,
                nuevoCliente.nombre,
                "CLIENTE"
            );
            Navigation.NavigateTo("/");
        }
        }
        finally
        {
            isProcessing = false;
        }
    }

    public class RegistroUsuario
    {
        [Required(ErrorMessage = "El nombre es obligatorio")]
        [MinLength(2)]
        public string Nombres { get; set; } = "";

        [Required(ErrorMessage = "El apellido paterno es obligatorio")]
        public string ApellidoPaterno { get; set; } = "";

        [Required(ErrorMessage = "El apellido materno es obligatorio")]
        public string ApellidoMaterno { get; set; } = "";

        [Required(ErrorMessage = "El DNI es obligatorio")]
        [RegularExpression(@"^\d{8}$", ErrorMessage = "El DNI debe contener exactamente 8 dígitos")]
        public string Dni { get; set; } = "";

        [Required(ErrorMessage = "El teléfono es obligatorio")]
        [RegularExpression(@"^\d{9}$", ErrorMessage = "El teléfono debe contener exactamente 9 dígitos")]
        public string Telefono { get; set; } = "";

        [Required(ErrorMessage = "El correo es obligatorio")]
        [EmailAddress(ErrorMessage = "Correo inválido")]
        public string Email { get; set; } = "";

        [Required(ErrorMessage = "Debe ingresar una contraseña")]
        [MinLength(8, ErrorMessage = "La contraseña debe tener mínimo 8 caracteres")]
        [ContrasenaFuerte(ErrorMessage = "La contraseña debe ser más segura (mínimo nivel fuerte). Usa mayúsculas, números o símbolos.")]
        public string Password { get; set; } = "";

        [Required(ErrorMessage = "Debe repetir la contraseña")]
        [Compare(nameof(Password), ErrorMessage = "Las contraseñas no coinciden")]
        public string ConfirmPassword { get; set; } = "";

        [Required(ErrorMessage = "Seleccione una fecha")]
        [MayorDeEdad(ErrorMessage = "Debe ingresar una fecha de nacimiento mayor a 18 años.")]
        public DateTime? FechaNacimiento { get; set; }
    }

    public class MayorDeEdadAttribute : ValidationAttribute
    {
        protected override ValidationResult? IsValid(object? value, ValidationContext validationContext)
        {
            if (value is DateTime fechaNac)
            {
                var edad = DateTime.Today.Year - fechaNac.Year;
                if (fechaNac.Date > DateTime.Today.AddYears(-edad)) edad--;
                if (edad < 18)
                {
                    return new ValidationResult(ErrorMessage ?? "Debe ser mayor de edad.");
                }
            }
            return ValidationResult.Success;
        }
    }

    public class ContrasenaFuerteAttribute : ValidationAttribute
    {
        protected override ValidationResult? IsValid(object? value, ValidationContext validationContext)
        {
            if (value is string password)
            {
                int score = 0;
                if (password.Length >= 8) score++;
                if (password.Any(char.IsUpper)) score++;
                if (password.Any(char.IsLower)) score++;
                if (password.Any(char.IsDigit)) score++;
                if (password.Any(ch => !char.IsLetterOrDigit(ch))) score++;

                if (score < 4)
                {
                    return new ValidationResult(ErrorMessage ?? "La contraseña debe ser más segura (nivel fuerte).");
                }
            }
            return ValidationResult.Success;
        }
    }
}
