<div class="user-menu-container">
    <button class="action-icon user-button" @onclick="ToggleMenu" @onfocusout="HideMenuDelayed">
        @if (IsLoggedIn && !string.IsNullOrEmpty(UserName))
        {
            <span class="user-name">@UserName</span>
        }

        @if (IsLoggedIn)
        {
            <i class="bi bi-person-circle"></i>
        }
        else
        {
            <a class="link_login" href="/login">
                <i class="bi bi-person-circle"></i>
            </a>
            
        }
    </button>

    <div class="dropdown-menu @(isMenuOpen ? "show" : "")">
        <ul class="menu-list">
            @if (IsLoggedIn)
            {
                <li>
                    <a href="/settings" class="menu-item">
                        <i class="bi bi-gear"></i> Settings
                    </a>
                </li>
                <li>
                    <a href="/logout" class="menu-item text-danger">
                        <i class="bi bi-box-arrow-right"></i> Log out
                    </a>
                </li>
            }
        </ul>
    </div>
</div>

@code {
    [Parameter]
    public bool IsLoggedIn { get; set; } = false; // Por defecto true para propósitos de demostración

    [Parameter]
    public string UserName { get; set; } = "Juan Perez"; // Nombre de prueba

    private bool isMenuOpen = false;

    private void ToggleMenu()
    {
        if (!IsLoggedIn) return;
        isMenuOpen = !isMenuOpen;
    }

    private async Task HideMenuDelayed()
    {
        // Pequeño retraso para permitir que los clics en los enlaces se registren antes de cerrar el menú
        await Task.Delay(200);
        isMenuOpen = false;
        StateHasChanged();
    }
}
